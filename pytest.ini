[pytest]
# Chỉ chạy bộ test tự động trong tests/ (bỏ qua các script chạy tay ở gốc:
# test_triage.py / test_chatbot.py / test_vnpt.py).
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
