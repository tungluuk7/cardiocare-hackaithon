[pytest]
# Chỉ chạy bộ test tự động trong tests/ (các script chạy tay nằm ở scripts/:
# test_triage.py / test_chatbot.py / test_vnpt.py).
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
