# Deps chung cả nhóm. Dùng >= thay vì == để build được trên Python mới (3.13/3.14):
# pydantic==2.6.4 KHÔNG có wheel cho Python 3.14 -> pip install -r sẽ fail khi build.
# Với >=, pip tự lấy bản mới nhất tương thích (có wheel sẵn).
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.9
python-dotenv>=1.0.1
httpx>=0.27.0
requests>=2.31.0
SQLAlchemy>=2.0.28

# Dev 1 — bắt buộc cho upload file (Form/File trong calls.py, chatbot.py)
python-multipart>=0.0.9

# Dev 1 — fallback giọng nói khi VNPT chưa sẵn sàng (tuỳ chọn)
gTTS>=2.5.1
SpeechRecognition>=3.10.4
